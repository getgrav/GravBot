const Hirez = require('./hirez.js')

let hirez = new Hirez({
  devId: '1319',
  authKey: 'B4C44966FFFA4915A2A9237AC9AC7F60'
})

hirez.smite.session.generate().then(() => {
  console.log(process.env.SMITE_PC_SESSION)
  hirez.smite.getPlayerAchievements(248884).then((res) => {
    console.log(res)
  })
})
